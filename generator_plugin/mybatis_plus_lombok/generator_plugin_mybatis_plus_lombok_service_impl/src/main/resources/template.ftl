

import org.springframework.stereotype.Service;

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;

/**
 * ${name} 表service接口实现
 *
 * @Description ${comment}
 * @author ${author}
 * @date ${createDate}
 * @version 1.0
 */
@Service
public class ${prefix}${className}${suffix} extends ServiceImpl<${mapperClassName}, ${entityClassName}> implements ${serviceInterfaceClassName}{

}